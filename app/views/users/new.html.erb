<% if current_user %>

	<%= form_for @user do |f| %>

	<div>
	    <%= f.label :address %>
	    <%= f.text_field :address %>
	</div>
	<div>
	    <%= f.label :city %>
	    <%= f.text_field :city %>
	</div>
	<div>
	    <%= f.label :zipcode %>
	    <%= f.text_field :zipcode %>
	</div>
	<div>
	    <%= f.label :state %>
	    <%= f.text_field :state %>
	</div>

	<%= f.submit "Add new user" %>

	<% end %>

<% else %> 

	<% link_to root_path %>

<%end%>

<!-- Some jquery to blackout the screen -->


<form class="form-horizontal">
<fieldset>

<!-- Form Name -->
<legend>SLINKY USER INFORMATION</legend>

<!-- Text input-->
<div class="control-group text-center">
  <label class="control-label" for="textinput"></label>
  <div class="controls">
    <input id="textinput" name="textinput" type="text" placeholder="ZIP" class="input-xlarge" required="">
    <p class="help-block">For Better Search Results, Enter Your Zip Code</p>
  </div>
</div>

</fieldset>
</form>
