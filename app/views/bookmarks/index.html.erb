<!DOCTYPE html>
<html>
<head>
  <title>Slinky Bookmarks</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
</head>
<body>

<h1 class="text-center">Bookmarks</h1>

<table class="table table-hover table-striped table-condensed">
  <thead>
    <tr class="row-margin">
      <th>Name</th>
      <th>Occupation</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
      <% @bookmarks.each do |bookmark| %>
        <% if current_user.id == bookmark.user_id %> 
          <tr>
            <td><%= bookmark.mark.name %></td>
            <td><%= bookmark.mark.description %></td>
            <td><%= link_to ' ', user_path(bookmark.mark.id), :class => "btn btn-success outline green-button fa fa-envelope-o fa-lg" %></td>
            <td><%= link_to ' ', bookmark, :class => "btn btn-danger outline red-button fa fa-trash-o fa-lg", method: :delete, data: { confirm: 'Are you sure?' } %></td>
          </tr>
        <% end %>
    <% end %>
  </tbody>
</table>
</body>
</html>